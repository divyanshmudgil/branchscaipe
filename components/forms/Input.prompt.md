Text field with focus ring and optional icons.

```jsx
<Input shape="pill" iconLeft={<Search size={18} />} placeholder="Search threads…" />
```

`shape`: rounded | pill. `invalid` switches to the error ring.
