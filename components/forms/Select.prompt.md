Custom dropdown with a soft elevated popover. Doubles as the merge "Choose parent" picker.

```jsx
<Select value={parent} onChange={setParent}
  options={[{value:'hooks',label:'Hooks'},{value:'closure',label:'Closure'}]} />
```
