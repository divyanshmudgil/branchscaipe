Glass dropdown action menu for context / overflow ("…") actions.

```jsx
<Menu trigger={<IconButton icon={<MoreHorizontal/>} label="More" />}
  items={[{id:'merge',label:'Merge to parent',icon:<GitMerge/>},{divider:true},{id:'del',label:'Delete',danger:true}]} />
```
