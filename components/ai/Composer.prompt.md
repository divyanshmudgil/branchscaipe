The message input bar — frosted glass pill, +, Tools, gradient Send (enabled only with text). Enter sends, Shift+Enter newlines. Shows a "Branching from …" chip when composing in a branch.

```jsx
<Composer value={v} onChange={e=>setV(e.target.value)} onSend={send} branchingFrom="Hooks" />
```
