Indented branch hierarchy tree for the navigator.

```jsx
<ThreadLineage activeId="edge" onSelect={go} tree={[
  {id:'hooks',label:'Hooks',children:[
    {id:'closure',label:'Closure',children:[{id:'example',label:'Example'},{id:'edge',label:'Edge case'}]},
    {id:'other',label:'Another topic'}]}]} />
```
