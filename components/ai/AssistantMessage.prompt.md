Assistant response with the product's signature hover action bar — Copy · Branch · Retry · Merge · More. `Branch` is iris-tinted and can show a "NEW" flag.

```jsx
<AssistantMessage actions={['copy','branch','retry','merge','more']} branchNew onAction={handle}>
  A closure is when a function remembers its outer scope…
</AssistantMessage>
```
