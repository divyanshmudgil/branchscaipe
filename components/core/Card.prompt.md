Rounded surface container for panels and content groups.

```jsx
<Card elevation="floating">…</Card>
<Card glass interactive>Frosted, lifts on hover</Card>
```

`elevation`: flat | raised | floating. `glass` turns on the backdrop-blur panel treatment. `interactive` adds a hover lift.
