{
  "name": "branchscaipe-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Branchscaipe backend API — streams chat completions from a pluggable AI provider (Gemini by default).",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@google/genai": "^2.13.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
